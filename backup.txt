import {Button, Card, CardContent, Grid, TextField, Typography } from "@mui/material"
import {useState, useEffect} from 'react'
import { useNavigate } from "react-router-dom";
import axios from '../api/axios';

const REGISTER_URL = '/api/v1/usuarios';

export default function Form() {
  const [nombre_usuario, setNombreUsuario] = useState('');
  const [email_usuario, setEmailUsuario] = useState('');
  const [password_usuario, setPasswordUsuario] = useState('');
  const [direccion_usuario, setDireccionUsuario] = useState('');
  const [path_doc, setPathDocUsuario] = useState('');
  const [telefono_usuario, setTelefonoUsuario] = useState('');

  const form = document.getElementById('form');

 const navigate = useNavigate()

  const handleSubmit = async(e) => {
  e.preventDefault();
  const formData = new FormData(form);

  console.log(...formData);
  }

  // const handleChange = e => 
  //   setUsuario({...usuario,[e.target.name]:e.target.value })
  


    return ( 
      <Grid container direction='column' alignItems='center' justifyContent='center'>
        <Grid item xs={3}>
          <Card sx={{mt:5}} style={{
            padding:'1rem'
          }}>
            <Typography textAlign="center" variant="5">Registro usuario</Typography>
            <CardContent>
              <form id="form">
                <label htmlFor="nombre">
                  Nombre:
                </label>
                <input
                  type="text"
                  id="nombre"
                  autoComplete="off"
                  onChange={(e) => setNombreUsuario(e.target.value)}
                  value={nombre_usuario}
                  required
                />


                <label htmlFor="direccion">
                  Dirección usuario:
                </label>
                <input
                  type="text"
                  id="direccion"
                  autoComplete="off"
                  onChange={(e) => setDireccionUsuario(e.target.value)}
                  value={direccion_usuario}
                  required
                />


                <label htmlFor="telefono">
                  Telefono:
                </label>
                <input
                  type="text"
                  id="telefono"
                  autoComplete="off"
                  onChange={(e) => setTelefonoUsuario(e.target.value)}
                  value={telefono_usuario}
                  required
                />


                {/* <label htmlFor="imagen">
                  Foto de perfil:
                </label>
                <input
                  type="file"
                  accept="image/*"
                  id="imagen"
                  autoComplete="off"
                  onChange={(e) => setPathDocUsuario(e.target.value)}
                  value={email_usuario}
                  required
                /> */}

                <label htmlFor="imagen">
                  Foto de perfil:
                </label>
                <input
                  type="text"
                  id="imagen"
                  autoComplete="off"
                  onChange={(e) => setPathDocUsuario(e.target.value)}
                  value={path_doc}
                  required
                />

              

                <label htmlFor="email">
                  Email:
                </label>
                <input
                  type="text"
                  id="email"
                  autoComplete="off"
                  onChange={(e) => setEmailUsuario(e.target.value)}
                  value={email_usuario}
                  required
                />


                <label htmlFor="password">
                  Contraseña:
                </label>
                <input
                  type="text"
                  id="password"
                  autoComplete="off"
                  onChange={(e) => setPasswordUsuario(e.target.value)}
                  value={password_usuario}
                  required
                />


                <Button variant="contained" type="submit" disabled={!nombre_usuario || !email_usuario || !password_usuario || !direccion_usuario || !path_doc || !telefono_usuario}>
                Guardar
              </Button>          
              </form>     
            </CardContent>
          </Card>
        </Grid>
      </Grid>
    )
  }